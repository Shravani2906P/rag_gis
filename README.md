# rag_gis

###Setup:

1).
->git clone https://github.com/yourusername/rag_gis.git
->cd rag_gis
->Install dependencies as : pip install -r requirements.txt
->Start server            : uvicorn api:app --reload                                      
                                              
2). Client side :
->Install dependencies as : npm i
npm run dev

3). CLI based :
python main.py



